services:
  web:
    image: php:8.2-apache
    container_name: apollo_web
    environment:
      APACHE_DOCUMENT_ROOT: /var/www/html
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
    env_file:
      - .env